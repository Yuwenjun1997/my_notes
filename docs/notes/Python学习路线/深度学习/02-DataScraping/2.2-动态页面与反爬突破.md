---
title: 2-动态页面与反爬突破
createTime: 2026/08/15 17:33:06
permalink: >-
  /notes/Python学习路线/shen-du-xue-xi/02-data-scraping/2-dong-tai-ye-mian-yu-fan-pa-tu-po/
---
# 动态页面爬取与反爬突破

## 一、动态页面与浏览器自动化

### 2.1 为什么需要浏览器自动化

很多现代网站的数据由 **JavaScript 异步渲染**生成，直接请求 HTML 拿不到内容。判断方式：

```python
# ❌ 直接用 requests 拿不到动态数据
import requests
resp = requests.get("https://example.com/spa")
# 页面里没有目标数据，因为数据是 JS 之后渲染的

# ✅ 方案一：先分析 XHR/API，直接请求后端接口
api_url = "https://example.com/api/books?page=1"
data = requests.get(api_url).json()

# ✅ 方案二：用浏览器自动化模拟真实浏览器渲染（见下文 Playwright）
```

**技术选型**：数据能否从接口拿到，优先走接口（更快更稳）；拿不到才用浏览器自动化。

### 2.2 Playwright 核心操作

Playwright 是现代浏览器自动化库，支持 Chromium / Firefox / WebKit，提供自动等待与网络拦截能力。

```bash
pip install playwright
playwright install chromium    # 安装浏览器内核
```

**页面访问与元素定位**

```python
import asyncio
from playwright.async_api import async_playwright


async def scrape_dynamic():
    async with async_playwright() as p:
        # 启动浏览器（headless 无头模式）
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()

        # 访问页面（wait_until='networkidle' 等网络空闲）
        await page.goto("https://example.com", wait_until="networkidle")

        # 等待元素出现：比固定 sleep 更可靠
        await page.wait_for_selector(".product-item", timeout=5000)

        # 定位元素：支持 CSS / XPath / 文本
        titles = await page.locator(".product-title").all_text_contents()
        prices = await page.locator(".price").all_text_contents()

        # 点击"加载更多"按钮触发懒加载
        load_more = page.locator("button:has-text('加载更多')")
        if await load_more.count():
            await load_more.click()

        # 滚动到底部触发滚动加载
        await page.mouse.wheel(0, 10000)
        await page.wait_for_timeout(1000)

        await browser.close()
        return titles, prices


# asyncio.run(scrape_dynamic())
```

**等待策略对比**

| 策略 | 用法 | 适用场景 |
|:-----|:-----|:---------|
| `wait_for_selector` | 等待元素出现在 DOM | 页面元素渲染 |
| `wait_for_load_state` | `domcontentloaded` / `networkidle` | 页面加载阶段 |
| `wait_for_timeout` | 固定等待 | 兜底，尽量少用 |
| `expect` 断言 | `expect(page).to_have_title()` | 断言与重试 |

**网络拦截与数据捕获**

```python
async def capture_api():
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()

        # 捕获所有 XHR/API 响应，直接拿 JSON
        async def on_response(response):
            if "/api/" in response.url and "json" in response.headers.get("content-type", ""):
                try:
                    data = await response.json()
                    print(response.url, data[:2])   # 打印接口数据
                except Exception:
                    pass

        page.on("response", on_response)
        await page.goto("https://example.com")
        await page.wait_for_timeout(3000)
        await browser.close()
```

**多标签页与上下文**

```python
# Context 之间完全隔离（Cookie/缓存/UA 相互独立），适合多账号
context1 = await browser.new_context(user_agent="Mozilla/5.0 (Windows)")
context2 = await browser.new_context(user_agent="Mozilla/5.0 (Macintosh)")

# 在同一个 context 内打开新标签页
page1 = await context1.new_page()
page2 = await context1.new_page()
await page1.goto("https://example.com")

# 处理新开标签页（如点击后弹出新窗口）
async with context1.expect_page() as new_page_info:
    await page1.click("a[target=_blank]")   # 触发新标签页
new_page = await new_page_info.value
await new_page.wait_for_load_state()
```

---

## 二、Selenium 与 Playwright 深入对比

### 2.3 两者差异与选型

| 维度 | Selenium | Playwright |
|:-----|:---------|:-----------|
| 浏览器支持 | Chrome/Firefox/Safari/Edge | Chromium/Firefox/WebKit |
| 安装 | 需要 WebDriver | 内置驱动，`playwright install` 即可 |
| 自动等待 | 需显式 `WebDriverWait` | 内置自动等待，默认开启 |
| 并发能力 | 较弱 | Context 隔离，天然支持并发 |
| 网络拦截 | 弱（需代理/插件） | 内置 `page.on("request"/"response")` |
| 反爬指纹 | 特征较明显（`navigator.webdriver=true`） | 可配置 `stealth`，指纹更接近真实 |
| 维护活跃度 | 一般 | 非常活跃（微软维护） |
| API 风格 | 同步为主，可异步 | 同步/异步双 API |

**新项目建议优先 Playwright**：自动等待 + 网络拦截 + 并发隔离三个核心能力开箱即用。

### 2.4 浏览器指纹与反检测

网站通过 JS 检测 `navigator.webdriver`、User-Agent、Canvas/WebGL 指纹等识别自动化。降低被识别概率的做法：

```python
from playwright.async_api import async_playwright


async def stealth_scrape():
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)

        # 注入 stealth 脚本，抹除自动化特征
        stealth_js = """
        Object.defineProperty(navigator, 'webdriver', { get: () => undefined });
        window.chrome = { runtime: {} };
        Object.defineProperty(navigator, 'languages', { get: () => ['zh-CN', 'zh'] });
        Object.defineProperty(navigator, 'plugins', {
            get: () => [1, 2, 3, 4, 5],
        });
        """
        context = await browser.new_context(
            user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0 Safari/537.36",
            viewport={"width": 1920, "height": 1080},
            locale="zh-CN",
            timezone_id="Asia/Shanghai",
        )
        await context.add_init_script(stealth_js)  # 在每个页面加载前执行

        page = await context.new_page()
        await page.goto("https://example.com")
        # 模拟真实用户行为：移动鼠标、偶尔滚动
        await page.mouse.move(500, 300)
        await page.wait_for_timeout(800)
        await browser.close()
```

**重要合规提醒**：反检测技术仅应用于**已获授权**的站点（如自有系统、公开 API、已同意采集的数据源）。强行突破验证码、绕过付费墙、抓取非公开数据可能违反服务条款与法律。

---

## 三、反爬对抗与合规策略

### 2.5 代理池与 IP 轮换

高频请求容易触发网站封 IP，常见应对是用**代理池**分散请求。

```python
# 代理中间件思路（配合 requests/httpx）
import random
import httpx

PROXY_POOL = [
    "http://user:pass@proxy1.example.com:8080",
    "http://user:pass@proxy2.example.com:8080",
    "http://user:pass@proxy3.example.com:8080",
]


def fetch_with_proxy(url):
    """每次请求随机挑选一个代理，失败则换下一个"""
    for _ in range(len(PROXY_POOL)):
        proxy = random.choice(PROXY_POOL)
        try:
            resp = httpx.get(url, proxy=proxy, timeout=10)
            if resp.status_code == 200:
                return resp
        except httpx.ProxyError:
            continue  # 该代理失效，换下一个
    raise RuntimeError("所有代理均失败")
```

**代理使用规范**

| 事项 | 建议 |
|:-----|:-----|
| 请求频率 | 加 `DOWNLOAD_DELAY` / `time.sleep` 随机延迟 |
| 失败重试 | 限制重试次数，避免放大流量 |
| 合规性 | 使用正规代理服务，遵守目标站点服务条款 |
| 数据范围 | 只采集公开、合法、非敏感数据 |

### 2.6 频率控制与限速

节流是**最基础也最重要的反封禁手段**：

```python
import time
import random


class RateLimiter:
    """简单限速器：控制每秒最大请求数"""

    def __init__(self, max_qps=2.0):
        self.max_qps = max_qps
        self.min_interval = 1.0 / max_qps
        self.last_request = 0.0

    def wait(self):
        # 距离上次请求不足最小间隔则等待
        now = time.monotonic()
        gap = self.last_request + self.min_interval - now
        if gap > 0:
            # 加上随机抖动，模拟人类节奏
            time.sleep(gap + random.uniform(0.1, 0.3))
        self.last_request = time.monotonic()


limiter = RateLimiter(max_qps=1.5)
for url in url_list:
    limiter.wait()
    resp = httpx.get(url)
```

**响应 429/403 的处理**

```python
if resp.status_code == 429:
    # 服务器要求限速，读取 Retry-After 头
    retry_after = int(resp.headers.get("Retry-After", "60"))
    time.sleep(retry_after)          # 等待后再继续
elif resp.status_code == 403:
    # 可能被封：切换代理 + 降低频率 + 更换 UA
    proxy = random.choice(PROXY_POOL)
```

### 2.7 验证码处理思路

处理验证码**必须基于授权场景**（登录自己的账号、测试自有系统）：

| 方案 | 说明 | 适用 |
|:-----|:-----|:-----|
| 人工介入 | 出现验证码时暂停并人工处理 | 低频、自用 |
| 第三方打码服务 | 调用打码平台 API 识别 | 有一定成本 |
| OCR 训练 | 用 Tesseract / CNN 识别简单验证码 | 简单图形验证码 |
| 行为规避 | 降低频率、放慢节奏，从源头减少验证码出现 | 长期更优 |

**人工介入方案示例**（用 input 暂停）：

```python
captcha_el = page.locator("#captcha")
if await captcha_el.count():
    print("出现验证码，请在浏览器中手动完成...")
    input("完成后按回车继续")   # 终端等待人工确认
```

---

## 四、合规与数据安全红线

### 2.8 爬虫的合规边界

采集数据前必须明确以下边界，**违反可能承担法律责任**：

1. **robots.txt**：尊重目标站的爬虫协议，禁用声明不可爬取的路径
2. **服务条款（ToS）**：采集行为不得违反目标站点的使用条款
3. **个人隐私**：不采集、不存储身份证号、手机号等敏感个人信息
4. **版权数据**：不抓取受版权保护的付费内容（影视、书籍、音乐）
5. **访问压力**：控制频率，不得对目标服务器造成拒绝服务

```python
# 尊重 robots.txt 的简单判断
import urllib.robotparser

rp = urllib.robotparser.RobotFileParser()
rp.set_url("https://example.com/robots.txt")
rp.read()

if rp.can_fetch("*", "https://example.com/private"):
    print("允许爬取")
else:
    print("robots.txt 禁止该路径，跳过")
```

### 2.9 数据清洗与存储安全

```python
import re


def sanitize(item):
    """采集后的数据清洗与脱敏"""
    # 去除 HTML 标签
    item["content"] = re.sub(r"<[^>]+>", "", item.get("content", ""))
    # 去除首尾空白
    item["title"] = item.get("title", "").strip()
    # 手机号脱敏：138****1234
    item["phone"] = re.sub(r"(\d{3})\d{4}(\d{4})", r"\1****\2", item.get("phone", ""))
    # 只保留合法字段，丢弃多余数据
    allowed = {"title", "content", "phone"}
    return {k: v for k, v in item.items() if k in allowed}
```

---

## 五、实践项目

### 项目目标

用 Playwright 爬取一个需要滚动加载的动态网站（如电商列表页），捕获其 XHR 接口数据，并配合限速与代理策略完成合规采集，输出 CSV。

**步骤**：

1. 分析目标站点：确认是动态渲染还是 API 直出，优先找 XHR 接口
2. 用 Playwright 的 `page.on("response")` 捕获接口 JSON
3. 实现 `RateLimiter` 控制请求频率，配置代理池
4. 处理滚动加载与"加载更多"按钮，拉取全部分页数据
5. 检查 `robots.txt`，确认采集合法
6. 数据清洗后写入 CSV，统计采集总量

**目录结构参考**：

```text
dynamic_scraper/
├── requirements.txt          # playwright、httpx
├── limiter.py                # 限速器
├── proxies.py                # 代理池
├── scraper.py                # Playwright 主采集脚本
├── sanitize.py               # 数据清洗与脱敏
└── output/
    └── products.csv          # 采集结果
```
